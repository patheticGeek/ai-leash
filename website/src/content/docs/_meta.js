export default {
  features: {
    title: "Features",
    // Expanded by default — it's the only real content section, no
    // reason to make it a click to reveal.
    theme: { collapsed: false },
  },
};
