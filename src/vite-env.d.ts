/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` at build time (`git rev-parse --short HEAD`).
declare const __COMMIT_HASH__: string;
// Injected by vite.config.ts's `define` at build time — see
// src/lib/buildChannel.ts.
declare const __BUILD_CHANNEL__: string;
